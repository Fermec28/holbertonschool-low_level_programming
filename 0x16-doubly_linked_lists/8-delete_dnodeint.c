#include "lists.h"

/**
 * delete_dnodeint_at_index - delete at specific index
 * @head: headof the list
 * @index: node index to be deleted
 * Return: 1 if was possible
 */
int delete_dnodeint_at_index(dlistint_t **head, unsigned int index)
{
	(void)head;
	(void)index;

	return (1);
}
